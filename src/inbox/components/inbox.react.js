var React = require('react');
var Basics = require('./admin-basics.react');
var Notifications = require('./admin-notifications.react')
var Payments = require('./admin-payments.react');
var Languages = require('./admin-languages.react');

module.exports = React.createClass({
  moreMessages: function() {

    // Get timestamp of highest message and send new call with this time

    // Then append new messages

  },
  componentDidMount: function() {

    // Generate date and make call for first batch of messages

  },
  render: function() {

    return (
       <div></div>
    )
  }
});
